# apple-action/deploy-cocoapods-version

Deploy Cocoapods version.

## Usage

```yml
- uses: ab180/apple-action/deploy-cocoapods-version
  with:
    podspec: ---.podspec # Path of podspec file
    token: --- # Token of CocoaPods (Default: $COCOAPODS_TRUNK_TOKEN)
```
